# ControlKernel

Interlateral Agents is the mesh starter. It does not include, install, or start
ControlKernel (the collab daemon, SQLite schema, migrations, or kernel tests).

ControlKernel is a separate project. This repository does not depend on it at
runtime. Do not add kernel services here because a roadmap or old tree mentioned
them.
